"""stores some attributes and paths"""

class config:
    """stores some essentail paths and attributes related to the repo
    
    These are common attribute that will be used, instead of parsing them again and again
    from the command line."""
    graph_data = "C:/Users/User/Desktop/deep learning/graph learning/graph_data.csv"
     # change it accoding to the location in your pcs
